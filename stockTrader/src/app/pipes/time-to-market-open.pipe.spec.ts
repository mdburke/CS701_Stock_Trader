import { TimeToMarketOpenPipe } from './time-to-market-open.pipe';

describe('TimeToMarketOpenPipe', () => {
  it('create an instance', () => {
    const pipe = new TimeToMarketOpenPipe();
    expect(pipe).toBeTruthy();
  });
});
